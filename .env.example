# Google OAuth Client ID (Web application type) used for "Save PDF to Google
# Drive" — see src/lib/googleAuth.js. Create one in Google Cloud Console under
# APIs & Services > Credentials, then set this in Vercel (Project Settings >
# Environment Variables) and locally in .env.local.
VITE_GOOGLE_CLIENT_ID=
